# Music Store

Web app for browsing generated song listings. Same seed returns the same catalog.

## Run locally

```bash
cd backend && dotnet run
cd frontend && npm install && npm run dev
```

Open http://localhost:3000

## Docker

```bash
docker compose up --build
```
